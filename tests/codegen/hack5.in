int main() {
    int x = 0, y = 0;
    while (x < 5) {
        x = x + 1;
        while (y < 5) {
            y = y + 1;
            if (x == y) continue;
            if (x + y > 6) break;
        }
    }
    return x + y;
}